# WeUsThem_Interview

This project is a react based app that uses materia-ui. <br>
To open the webpage you you must navigate to the project and then to the Frontend folder with cd Frontend 
After that use npm start to open the web page.
I was unable to set up a database and server but included the what would be the basic functions of the database.
For the spam filter I did not code it but it would be a a filter that only allows specific domain name emails to be sent as well as having a limit to how mayn could be sent within a specific time period.
